# Git Basic Lab

Dự án web tĩnh đơn giản để thực hành Git cơ bản.

## Files
- `index.html`: giao diện trang web
- `css/style.css`: style cơ bản
- `js/app.js`: JavaScript đơn giản

## Suggested first commit
```bash
git init
git add .
git commit -m "Initial commit: add starter website"
```
